#include "main.h"

/**
 * add - sum up two integers
 * @a: are the argument for function
 * @b: are the argument for function
 * Return: 0 (successful)
 */

int add(int a, int b)
{
	sum = a + b;
	_putchar(sum);
	return (0);
}
