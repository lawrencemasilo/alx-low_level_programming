#include <stdio.h>

/**
 * main - Entry point
 * Description: 'This program prints out the alphabet'
 * Return: 0 (successful)
 */

int main(void)
{
	int a;
	for (a = 65; a <= 90; a++)
	{
		putchar(a);
	}
	return (0);
}
